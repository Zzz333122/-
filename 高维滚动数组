//p1025
innline int pos(const int &x)
{
      return x%N+1; // N视题目而定 一般是几维就是多少
}
//更快的方法 利用指针数组 先预处理每个指针数组的指向 然后直接跑即可
int dp[610][610];
int *f[200100];
int p = 0;
for (int i = 0; i <= n; ++i)
{
      if (p >= 600)
      p -= 600;
      f[i] = dp[p + 1];
      ++p;
}
